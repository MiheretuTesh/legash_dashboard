import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  container: {
    display: "grid",
    gridTemplateColumns: "auto auto",
    gap: "30px",
    marginTop: "54px",
  },
});
